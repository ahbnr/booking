const FrontendConfig = {
  language: 'de',
  organization: 'My Organization',
  appBarTitle: '',
  // DEBUG OPTION: This address will be set as default for all bookings, if not undefined
  fixedBookingMailTarget: undefined,
};

export default FrontendConfig;
